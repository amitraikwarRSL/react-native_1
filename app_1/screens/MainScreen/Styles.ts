import { StyleSheet } from "react-native";

/**
 * Styles definition for the mainscreen component.
 */
export const style = StyleSheet.create({
  container: {
    flex: 1,
    flexDirection: "column"
  },
  contentContainer:{
    height:"auto",
    backgroundColor: "#AAAAAA"
  }
});

