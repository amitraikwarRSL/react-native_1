import { StyleSheet } from "react-native";

/**
 * Style definition for the top app bar component.
 */
export const style = StyleSheet.create({
    container:{
        height: 60,
        justifyContent: "center",
        margin: 5,
        borderRadius: 10,
        padding: 5,
        backgroundColor: "#2f49cf"
    },
    appBarText:{
    textAlign: "center",
    fontSize: 20,
    paddingVertical: 4,
    color:"#ffffff"
    }
});